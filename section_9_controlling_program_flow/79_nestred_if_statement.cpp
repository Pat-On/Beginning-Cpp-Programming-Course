/*


        If (expr1)
            if (expr2)
                statement1;
            else
                statement2;

    if statement is nested within another
    allows testing of multiple conditions
    else belongs to the closes if




*/
#include <iostream>
#include <iomanip>

using namespace std;

int main()
{

    cout << fixed << setprecision(2); // print things nicer

    return 0;
}